fastapi[standard]                   # web server
pydantic                            # validation of user requests
sqlalchemy                          # ORM
jinja2                              # template engine the same one used by Flask and other tools.